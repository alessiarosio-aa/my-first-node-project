// questo è il file che viene avviato dal server
console.log("node is running") // se andiamo su github desktop - repositoyr - open in command prompt, ci fa vedere se la pagina viene elaborata dal server scrivendo "node server.js" (senza % prima della frase)
